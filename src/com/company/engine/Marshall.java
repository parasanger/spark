package com.company.engine;

import com.sun.source.tree.BinaryTree;
import com.sun.tools.javac.util.Pair;

import java.util.LinkedList;

/**
 * Created by rogerwarner on 12/22/16.
 *
 * take arg strings and process them into pairs
 */
public class Marshall {
    /**
     * handles break the string into tokens, then into ops and data times then figuring who goes with who
     * and then structures that into a tree of pairs/ops
     * @param array
     * @return
     */
    BinaryTree parseMe(String[] array) {
        return null;
    }
}
